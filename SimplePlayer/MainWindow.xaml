<Window x:Class="SimplePlayer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SimplePlayer"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="200" />
        </Grid.RowDefinitions>

        <local:SimpleHost x:Name="PlayerHost" Player="{Binding Path=Player}">
            <local:SimpleHost.OverlayTemplate>
                <ControlTemplate>
                    <Border BorderBrush="Red" BorderThickness="5">
                        <TextBlock Foreground="Red" Text="{Binding Path=Text}" />
                    </Border>
                </ControlTemplate>
            </local:SimpleHost.OverlayTemplate>
        </local:SimpleHost>

        <StackPanel Grid.Row="1">
            <TextBox />
        </StackPanel>
    </Grid>
</Window>
